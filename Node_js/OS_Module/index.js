const os = require('os');
console.log(os.arch()); // CPU Architecture EX. 32 / 64 bits.
console.log(os.hostname()); // System name
console.log(os.platform()); // Windows version
console.log(os.tmpdir()); // temp file path
console.log(os.type()); // Operater system

